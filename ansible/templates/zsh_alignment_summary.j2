# ZSH Cluster Alignment Test Results

**Date:** {{ test_date }} {{ test_time }}  
**Test Type:** User Experience Validation  
**Methodology:** Real shell session testing

## Summary

| Node | Status | Score | Issues |
|------|--------|-------|--------|
{% for host in groups['all'] %}
| {{ host }} | {{ 'PASS ✅' if hostvars[host]['zsh_test_passed'] else 'FAIL ❌' }} | {{ hostvars[host]['zsh_test_score'] }}/10 | {{ hostvars[host]['zsh_test_failures'] }} |
{% endfor %}

## Overall Cluster Status

{% set passing_nodes = groups['all'] | select('extract', hostvars, 'zsh_test_passed') | list %}
{% set total_nodes = groups['all'] | length %}
**Alignment Status:** {% if passing_nodes | length == total_nodes %}PERFECT ✅{% else %}ISSUES ❌{% endif %}

**Nodes Passing:** {{ passing_nodes | length }}/{{ total_nodes }}

## What This Test Actually Validated

1. **Real ZSH functionality** - Not just file existence
2. **User experience** - What happens when someone opens a shell  
3. **Interactive features** - Aliases, prompts, tools actually work
4. **Configuration loading** - Configs actually get sourced properly
5. **Tool integration** - Modern CLI tools are functional
6. **Cross-node consistency** - Same experience everywhere

## Failed Tests (if any)

{% for host in groups['all'] %}
{% if not hostvars[host]['zsh_test_passed'] %}
### {{ host }} Issues
See detailed results in: `{{ host }}_results.txt`
{% endif %}
{% endfor %}

---
*Generated by ZSH Ultimate Alignment Test*