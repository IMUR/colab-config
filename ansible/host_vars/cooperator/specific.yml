# Host-specific variables for cooperator only
# These override everything except playbook vars and command line

# Cooperator-specific services
pihole_enabled: true
caddy_sites:
  - dns.ism.la
  - mng.ism.la
  - cfg.ism.la
  - ssh.ism.la

# Override example - this would override group_vars
# ansible_user: cooperator_special  # <-- uncomment to see precedence