# SpecStory auto-generated files and backups
.specstory/
# Archive directory (moved/deleted files)
.archive/
# Large binary files (same as .gitignore for consistency)
containerization/**/*.tar
*.tar.gz
*.zip